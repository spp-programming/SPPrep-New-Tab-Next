# make-zip
Creates a zip file locally that can be uploaded directly to the Chrome Web Store. It ensures that only the prepared source files are included at the root of the archive.

> [!IMPORTANT]
> You need to run `prepare-extension` first to set the configuration as either staff or student before using this tool.

## Usage
```bash
node make-zip.js
```